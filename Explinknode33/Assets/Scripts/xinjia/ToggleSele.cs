using UnityEngine;
using System.Collections;
using UnityEngine.UI;
public class ToggleSele : MonoBehaviour {
	private SelectionControl selectionControl;
	// Use this for initialization
	void Start () {
//		selectionControl = gameObject.GetComponentInParent<SelectionControl> ();
//		gameObject.GetComponent<Toggle> ().onValueChanged.AddListener (delegate {
//			selectionControl.ToggleValueChange();
//		});
//		//gameObject.GetComponent<Toggle> ().group=gameObject.GetComponentInParent<ToggleGroup> ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
